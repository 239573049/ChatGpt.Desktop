@using BlazorComponent.I18n
@namespace ChatGpt.Shared
@inject I18n I18n
<MFooter App
         Color="transparent"
         Height="72"
         Inset>
    <MTextField BackgroundColor="@(ChatGptOptions.Dark?"#ffffff":"grey lighten-1")"
                Dense
                Flat
                HideDetails="@("true")"
                Class="@(ChatGptOptions.Dark?"dark":"white")"
                @bind-Value="value"
                OnKeyDown="async (e) => await KeySubmit(e)"
                Solo></MTextField>

    <MTooltip Top>
        <ActivatorContent>
            <MButton Style="margin-left:8px;" @attributes="@context.Attrs" Text OnClick="OnClick">
                <MIcon>fas fa-solid fa-paper-plane</MIcon>
            </MButton>
        </ActivatorContent>
        <ChildContent>
            @I18n.T("SendMessageHint")
        </ChildContent>
    </MTooltip>

</MFooter>

<style>
    .dark input {
        color: black !important;
    }

    .white input {
        color: aliceblue !important;
    }
</style>