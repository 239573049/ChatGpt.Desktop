@namespace ChatGpt.Shared

<MFooter App
         Color="transparent"
         Height="72"
         Inset>
    <MTextField BackgroundColor="@(ChatGptOptions.Dark?"grey lighten-1":"#333333")"
                Dense
                Flat
                HideDetails="@("true")"
                @bind-Value="value"
                OnKeyDown="async (e) => await KeySubmit(e)"
                Solo></MTextField>
    <MButton Text OnClick="OnClick">发送</MButton>
</MFooter>
